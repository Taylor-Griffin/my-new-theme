{% assign mainProduct = all_products['fitra-activity-band'] %}

{{ mainProduct.title }}
{{ mainProduct.description }}
{% for product in mainProduct.images %}
  <img src="{{ product | image_url: width: 450, height: 450 }} loading='lazy'">
{% endfor %}
